interface:
  display_name: "Pocketbase"
  short_description: "Repo-specific patterns and workflows for pocketbase"
  default_prompt: "Use the pocketbase repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true